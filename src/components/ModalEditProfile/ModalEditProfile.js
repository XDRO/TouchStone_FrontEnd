import "./ModalEditProfile.css";
import { Modal } from "../Modal/Modal";
export const ModalEditProfile = () => {
  // add this opotion at the bottom of the users profile page
  // inside of profile sideBar content
  //   think more about what you want to add then come back
  return (
    <Modal>
      <div></div>
    </Modal>
  );
};
